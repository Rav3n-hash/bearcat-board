import axios from "axios";

async function loginUser(email, password) { /* Validates user credentials */ }
async function verifyToken(token) { /* Checks if the user is logged in */ }

export{/*Export all functions*/}